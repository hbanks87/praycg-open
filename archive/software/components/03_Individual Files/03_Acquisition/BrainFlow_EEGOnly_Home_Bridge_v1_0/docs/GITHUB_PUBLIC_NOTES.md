# GitHub Public Notes

Suggested repository location:

```text
praycg-open/software/acquisition_brainflow_eeg_only/
```

Public description:

```text
A simplified BrainFlow-to-LSL EEG bridge for PR-AYC-G home users who do not use the ALS-PT19 / photodiode timing sensor.
```

Recommended README boundary:

```text
This bridge streams EEG and bridge status markers only. It does not validate physical screen timing. For timing-critical studies, use the ALS-enabled acquisition stack.
```

Do not upload:

```text
private XDF files
participant names
commercial stimulus media
local filesystem paths with private information
```

# PRAYCG Offline Interpretive Report

Schema: `PRAYCG_Offline_Interpretive_Report_v1_5_5`  
Version: `1.5.5`  
Run label: `FieldTest`  
Analysis folder: `/mnt/data/PRAYCG_FieldOfDreams_Run1_MasterComprehensive_FULL_v1_1`

## Boundary
This offline report is a rule-based interpretation aid. It summarizes available module outputs. It does not prove consciousness, memory formation, OSM biology, hidden-Y mechanisms, clinical effects, or literal thermodynamic entropy. Self-report is treated as a contextual evidence stream, not proof of internal state.

## Recommended interpretation hierarchy

**BOXED PRIMARY PATH:** Timing/QC + StimulusFingerprint/CET-R + artifact/confound gates → A-MRED / MRED-Peak-Resolution endpoint compression.

**SECONDARY:** NIP/BIT/CII/IAQ, TTI, NUPI.

**EXPLORATORY / CONVERGENCE:** KHT-topo, NAST, EET, MRED-ITP/ACG/OCU, OCM/RSM/CVB/Squint, LSO where applicable.

## 1. Data and QC status

The module registry was found: `field_full_suite_module_registry.csv`.
No ALS start-pulse QC table was found. Timing-grade claims should be kept cautious unless another timing report exists.

## 2. Boxed primary endpoint path: A-MRED

A-MRED evaluated **14** anchor rows and found **1** A-MRED pass rows.
Passing/candidate anchors: `FOD_BLIND_A6`.
Claim-level labels on passing rows: `runner_registered_estimated_blind_not_frame_verified`.
Caution: at least one pass is timing- or claim-level-cautioned. Treat it as pilot-positive unless frame-verified timing and timing QC are clean.

## 3. MRED-Peak vs MRED-Resolution

Anchors evaluated: **7**.
Strict or strict-cautioned MRED-Peak passes: **1**.
Soft MRED-Peak candidates: **0**.
MRED-Resolution candidates: **0**.

MRED-Peak dominant: acute Target-specific recognition/integration is stronger than delayed resolution.

Top peak candidate: `FOD_BLIND_A6` with score `1.000` and gate `STRICT_PEAK_PASS_TIMING_CAUTION`.
Top resolution candidate: `FOD_BLIND_A6` with score `0.500` and gate `NO_RESOLUTION_PASS`.

## 4. Secondary summary modules

TTI: `0.568` — Target-favoring reception/extraction contrast.
CII mean by condition: control `0.000`, override `0.010`, target `0.063`.
CET/CET-R model table found. Review R² / blocked-CV columns to judge whether stimulus regressors explain the physiological proxy. Negative blocked-CV generally means poor generalization.

## 5. Exploratory / convergence modules

CandidateLocal KHT-topo/MRED event table found. Treat this as exploratory coupling/convergence unless promoted through A-MRED or BIT-style gates.
EET echo table found. Maximum state-vector similarity observed: `0.925`. This is after-state resemblance, not proof of replay or memory.
MRED-ITP table found. ACG/OCU complexity/blink markers are convergence aids, not proof of thermodynamic entropy reduction or memory encoding.

## 6. Self-report and confound context

Interpretive boundary: self-report can constrain or contextualize physiology, but it does not prove internal state or mechanism.

## 7. Next-step guidance

- Treat strict A-MRED/MRED-Peak passes as strongest only when anchors were locked before acquisition and timing QC passed.
- Treat MRED-Resolution as strongest when Baseline2, EET/NUPI, autonomic recovery, and self-report echo converge.
- If gamma artifacts, missing ALS, missing EOG/EMG, or unverified anchor timing are present, lower the claim grade.
- Use this report as a map of the output folder, not as a substitute for visual inspection and replication.
